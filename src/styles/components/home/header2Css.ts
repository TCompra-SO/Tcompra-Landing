import styled from "styled-components";

const Header2Css = styled.h3`
  text-align: left;
  position: relative;
  font-size: 1.5em;
  color: white;
  z-index: 10;

  @media (max-width: 850px) {
    font-size: 2em;
    padding: 0 0 0 0;
  }
`;

export { Header2Css };
