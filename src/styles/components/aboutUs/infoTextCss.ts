import styled from "styled-components";

const InfoTextCss = styled.p`
  visibility: visible;
  animation-delay: 0.5s;
  font-weight: 500;
  margin: 0px;
  transition: all 0.4s ease-in-out;
  line-height: 28px;
  padding-bottom: 1em;
  text-align: left;
  color: var(--text);
`;

export default InfoTextCss;
